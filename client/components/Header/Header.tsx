
import Menu from '../Menu/Menu'
import { Layout } from 'antd';

const { Header } = Layout;

const Index= () => {

    return (
      <Header>
        BigTodo
        <Menu />
      </Header>
    );
  }

  export default Index